<Page x:Class="GUI_WPF.Pages.Dashboard.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GUI_WPF.Pages.Dashboard"
      xmlns:cards="clr-namespace:GUI_WPF.UserControls.Cards"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DashboardPage">

    <Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- STATS -->
                <UniformGrid Columns="4" Margin="0,0,0,24">
                    <cards:StatCard Title="Total Users" Value="12,482" Icon="👥" Trend="+12.4%" Description="vs last month"/>
                    <cards:StatCard Title="Bookings" Value="4,281" Icon="📅" Trend="+8.2%" Description="new reservations"/>
                    <cards:StatCard Title="Revenue" Value="$84K" Icon="💳" Trend="+21.3%" Description="monthly revenue"/>
                    <cards:StatCard Title="Occupancy" Value="78%" Icon="🏡" Trend="+5.1%" Description="room utilization"/>
                </UniformGrid>
                <!-- CONTENT CARD -->
                <Border Grid.Row="2" Style="{StaticResource GlassCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Recent Activity" FontSize="24" FontWeight="Bold" Foreground="#3A3131"/>
                        <TextBlock Text="Latest actions across the platform" Margin="0,6,0,30" Foreground="#8B7D7D"/>
                        <!-- ACTIVITY ITEM -->
                        <Border Background="#FFF8F4" CornerRadius="20" Padding="18" Margin="0,0,0,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="46" Height="46" Background="#FFF0F0" CornerRadius="23">
                                    <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="New host registration approved" FontWeight="SemiBold" Foreground="#3A3131"/>
                                    <TextBlock Text="2 minutes ago" Margin="0,4,0,0" Foreground="#8B7D7D" FontSize="12"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
